job('example-file-upload-job') {
    parameters {
        fileParam('UPLOAD_FILE', 'Select a file to upload')
    }
    steps {
        shell('echo "Uploaded file name: ${UPLOAD_FILE}"')
        shell('ls -la')
    }
}
